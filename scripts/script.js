window.onload=()=>{
    // declarables
    let cards=['html','css','js','css','js','html'];
    // functions
    // suffle cards order
    const shuffle=()=>{
        cards = cards.sort(() => 0.5 - Math.random());
    }
    
}